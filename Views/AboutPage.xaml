<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NetGui.ViewModels"
             x:Class="NetGui.Views.AboutPage"
             x:DataType="vm:MainViewModel"
             BackgroundColor="#1a1a1a"
             Title="About SC64 Manager">
    
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="25" HorizontalOptions="Center">
            
            <!-- Official Logo -->
            <Image Source="sc64.png" 
                   HeightRequest="150" 
                   WidthRequest="150" 
                   HorizontalOptions="Center" 
                   Margin="0,20,0,10"/>

            <Label Text="SC64 Gui Manager"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center" />

            <Label Text="Version 2.2.1 (Codename: The Carousel Update)"
                   FontSize="14"
                   TextColor="#888888"
                   HorizontalOptions="Center" />

            <BoxView HeightRequest="1" BackgroundColor="#333333" WidthRequest="300" Margin="0,10" />

            <Label Text="A modern, high-performance management tool for the SummerCart64."
                   FontSize="16"
                   TextColor="#cccccc"
                   HorizontalTextAlignment="Center"
                   MaximumWidthRequest="450" />

            <!-- Credits -->
            <VerticalStackLayout Spacing="5" Margin="0,15,0,0">
                <Label Text="Developed by Kevin Meerts"
                       FontSize="14"
                       TextColor="White"
                       HorizontalOptions="Center" />
                <Label Text="[https://github.com/KM198912]"
                       FontSize="12"
                       TextColor="#0078d4"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Links -->
            <VerticalStackLayout Spacing="10" Margin="0,20,0,0">
                <Button Text="Official SummerCart64 Website"
                        Clicked="OnOfficialSiteClicked"
                        BackgroundColor="#252525"
                        TextColor="White"
                        Padding="20,10" />
                
                <Button Text="Project GitHub Repository"
                        Clicked="OnRepoClicked"
                        BackgroundColor="#252525"
                        TextColor="White"
                        Padding="20,10" />

                <Button Text="View Help &amp; Documentation"
                        Command="{Binding OpenHelpCommand}"
                        BackgroundColor="#0078d4"
                        TextColor="White"
                        FontAttributes="Bold"
                        Padding="20,10" />
            </VerticalStackLayout>

            <Label Text="Hardware designed by Polprzewodnikowy"
                   FontSize="12"
                   TextColor="#888888"
                   HorizontalOptions="Center"
                   Margin="0,30,0,0" />
            <Label Text="Not Affiliated with SummerCart64"
                   FontSize="14"
                   FontAttributes="Bold"
                   TextColor="#bdc3c7"
                   HorizontalOptions="Center"
                   Margin="0,30,0,5" />
            <Label Text="This project is an independent hobby project created to make managing the SummerCart64 easier. It is not affiliated with, endorsed by, or associated with the official SummerCart64 hardware developers."
                   FontSize="12"
                   TextColor="#888888"
                   HorizontalTextAlignment="Center"
                   HorizontalOptions="Center"
                   MaximumWidthRequest="400"
                   Margin="0,0,0,30" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
